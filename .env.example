# Skill Factory API
PORT=8787
HOST=127.0.0.1

# Phase 4 — LLM for exploration (optional; use only on server, never commit real keys)
# Precedence: ~/Downloads/OpenAI-API.md → apps/skill-factory-api/api.md → env (Downloads first avoids stale shell keys)
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.apiyi.com/v1
# OPENAI_MODEL=gpt-4o-mini

# MCP Harness Sync skill (apps/mcp-harness-sync) — where to pull installers from
# SKILL_FACTORY_API_BASE=http://127.0.0.1:8787
# HARNESS_SYNC_USER_ID=chrome-extension

# Phase 6 — base URL for temporary install links (replace in production)
PUBLIC_SKILL_BASE=https://skills.example.com
