{
  "name": "@skill-factory/mcp-harness-sync",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP skill: pull Skill Factory harness installer published by the Chrome extension (GET /v1/sync/harness-installer).",
  "bin": {
    "mcp-harness-sync": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.7.2"
  }
}
