{
  "name": "maton-browse-gateway-monorepo",
  "private": true,
  "version": "0.2.0",
  "description": "Chrome extension: browsing capture + matonPlan JSON for Maton/ClawHub API Gateway. Extended skill in skills/clawhub-api-gateway-browse/. Legacy Skill Factory API is under archive/.",
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "relay": "npm run start --workspace=@skill-factory/maton-local-relay",
    "install-native-host": "node apps/maton-native-host/scripts/install-native-host.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.14.2",
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}
